export const firebase = (state) => state.firebase.firebase;

export const experienceManagerOffices = (state) => state.auth.experienceManagerOffices;

export const rehydrated = (state) => state._persist.rehydrated;

export const mixpanel = (state) => state.firebase.mixpanel;
